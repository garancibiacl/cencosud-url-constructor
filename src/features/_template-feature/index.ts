/**
 * _template-feature — public API
 *
 * Rename this when creating a real feature.
 * Only export what the router and other features need.
 * Never import from internal paths — always go through this barrel.
 */
export { default as TemplatePage } from "./ui/TemplatePage";
