/**
 * Kept for reference only — routing is now handled by src/app/router.tsx.
 * This file is no longer referenced.
 */
import { Navigate } from "react-router-dom";

const Index = () => <Navigate to="/constructor-url" replace />;

export default Index;
